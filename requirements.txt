# Core dependencies
pyyaml>=6.0

# Optional: neural-memory (install if available, mock mode if not)
# neural-memory>=0.8.0

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
flake8>=6.1.0
